<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- Top AppBar (BUILD STUDIO) -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface"
        app:elevation="0dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="@string/app_name"
            app:titleTextColor="#111827"
            app:titleTextAppearance="@style/TextAppearance.Material3.TitleLarge"
            app:menu="@menu/menu_main" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/card_border" />

    </com.google.android.material.appbar.AppBarLayout>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- Projects Header -->
        <TextView
            android:id="@+id/tv_header_projects"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/projects"
            android:textColor="#111827"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginStart="24dp"
            android:layout_marginTop="24dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Projects RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_projects"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:padding="16dp"
            android:clipToPadding="false"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_header_projects"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Centered Empty State: "No Projects Found" -->
        <TextView
            android:id="@+id/tv_no_projects"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/no_projects_found"
            android:textColor="#111827"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginTop="60dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_header_projects" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Speed Dial FAB Container -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:orientation="vertical"
        android:gravity="end"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="24dp">

        <!-- Speed Dial Option: "Create New Project" Pill + Mini FAB -->
        <LinearLayout
            android:id="@+id/layout_speed_dial_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp"
            android:visibility="gone">

            <!-- Green Pill Label -->
            <TextView
                android:id="@+id/btn_create_new_project_pill"
                android:layout_width="wrap_content"
                android:layout_height="44dp"
                android:background="@drawable/bg_pill_green"
                android:text="@string/create_new_project"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                android:textSize="14sp"
                android:gravity="center"
                android:paddingStart="18dp"
                android:paddingEnd="18dp"
                android:layout_marginEnd="10dp"
                android:elevation="4dp" />

            <!-- Circular Green Mini FAB -->
            <ImageView
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="@drawable/bg_btn_green"
                android:src="@android:drawable/ic_input_add"
                android:padding="10dp"
                app:tint="#FFFFFF"
                android:elevation="4dp" />

        </LinearLayout>

        <!-- Main Purple Squircle FAB '+' -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_main"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@android:drawable/ic_input_add"
            app:tint="#FFFFFF"
            app:backgroundTint="@color/primary"
            app:elevation="6dp"
            app:fabCustomSize="60dp"
            app:maxImageSize="28dp"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Medium" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
